name: Deploy
on:
  workflow_dispatch:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: [self-hosted, openclaw]
    steps:
      - name: Pull latest
        run: cd ~/PromptForge && git pull origin main

      - name: Build image
        run: cd ~/PromptForge && docker build -t promptforge:latest .

      - name: Update service
        run: docker service update --force warren_promptforge
